apply plugin: 'java'

dependencies {
    compile files('lib/junit-4.11.jar')
}

sourceSets {
    main {
        java {
            srcDir 'src'
        }
    }

    test {
        java {
            srcDir 'test'
        }
    }
}
